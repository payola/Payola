package cz.payola.web.shared

import cz.payola.common.entities.User

@remote object DomainData
{
    def searchMembersAvailableForGroup(groupId: Int, needle: String) = {
        //df.getMembersAvailableForGroup(groupId)
    }

    def searchDataSources(needle: String) = {

    }
}
